apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uber-ingress
spec:
  rules:
  - host: uber.local  # Or your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gateway
            port:
              number: 3000